<UserControl
    x:Class="Dash.Util.CourtesyDocuments.DBFilterChart"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10 10 10 0" Grid.Row="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" HorizontalAlignment="Stretch" Margin="20 0 20 0" VerticalAlignment="Bottom" Height="30" Background="SteelBlue"/>
            <Grid Grid.Column="1" HorizontalAlignment="Stretch" Margin="20 0 20 0" VerticalAlignment="Bottom" Height="30" Background="SteelBlue"/>
        </Grid>
        <Grid x:Name="LineGrid" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="5 0 5 0">
            <Line StrokeThickness="3" Stroke="Black" X1="0" Y1="0" X2="{Binding ElementName=LineGrid,Path=ActualWidth}" Y2="0"></Line>
            <TextBlock x:Name="xParameter" Text="Parameter" Foreground="Black" FontSize="12" Width="100" TextAlignment="Center" HorizontalAlignment="Center" Height="100"></TextBlock>
        </Grid>
    </Grid>
</UserControl>
