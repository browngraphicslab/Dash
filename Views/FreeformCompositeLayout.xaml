<UserControl
    x:Class="Dash.Views.FreeformCompositeLayout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dash="using:Dash"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="XGrid" SizeChanged="XGrid_OnSizeChanged" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <ItemsControl x:Name="xItemsControl" 
                      x:FieldModifier="public" 
                      ItemsSource="{Binding DataBindingSource}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas x:Name="CollectionCanvas" 
                            x:FieldModifier="public"
                            Background="Transparent"
                            Width ="0" 
                            Height="0" 
                            VerticalAlignment="Top" 
                            HorizontalAlignment="Left">
                    </Canvas>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            </ItemsControl>
        <Grid.Clip>
            <RectangleGeometry x:Name="XClipRect"></RectangleGeometry>
        </Grid.Clip>
    </Grid>
</UserControl>
