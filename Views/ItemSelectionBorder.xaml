<UserControl
    x:Class="Dash.Views.ItemSelectionBorder"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Border BorderThickness="2">
        <Border.BorderBrush>
            <SolidColorBrush Color="{ThemeResource SystemListMediumColor}"/>
        </Border.BorderBrush>
        <Button  VerticalAlignment="Top" HorizontalAlignment="Right" Width="20" Height="20" FocusVisualPrimaryBrush="{ThemeResource ButtonBorderThemeBrush}" Padding="2">
            <Button.Background>
                <SolidColorBrush Color="{ThemeResource SystemListMediumColor}"/>
            </Button.Background>
            <Button.Content>
                <SymbolIcon Foreground="{StaticResource WindowsBlue}">
                    <SymbolIcon.Symbol>Accept</SymbolIcon.Symbol>
                </SymbolIcon>
            </Button.Content>

        </Button>

    </Border>
</UserControl>
