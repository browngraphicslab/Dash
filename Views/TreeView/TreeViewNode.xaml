<UserControl
    x:Class="Dash.Views.TreeView.TreeViewNode"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash.Views.TreeView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    DataContextChanged="TreeViewNode_OnDataContextChanged">
    <UserControl.Resources>
        <local:ExpandedToArrowConverter x:Key="ExpandedToArrowConverter"></local:ExpandedToArrowConverter>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock x:Name="XArrowBlock" FontFamily="{StaticResource FontAwesome}" Text="{x:Bind IsExpanded, Converter={StaticResource ExpandedToArrowConverter}, Mode=OneWay}" Tapped="XArrowBlock_OnTapped" Visibility="{x:Bind IsCollection, Mode=OneWay}"></TextBlock>
        <TextBlock x:Name="XTitleBlock" Grid.Column="1" Grid.Row="0"></TextBlock>
        <local:TreeViewList x:Name="XTreeViewList" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Visibility="{x:Bind IsExpanded, Mode=OneWay}"></local:TreeViewList>
    </Grid>
</UserControl>
