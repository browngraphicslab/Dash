<UserControl
    x:Class="Dash.Views.TreeView.TreeViewList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash.Views.TreeView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="XList"
    DataContextChanged="TreeView_OnDataContextChanged"
    Loaded="TreeView_OnLoaded"
    Unloaded="TreeView_OnUnloaded">

    <Grid>
        <ItemsControl ItemsSource="{x:Bind ViewModel.BindableDocumentViewModels, Mode=OneWay}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:TreeViewNode FilterFunc="{Binding ElementName=XList, Path=FilterFunc}"></local:TreeViewNode>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
