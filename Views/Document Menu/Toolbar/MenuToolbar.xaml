<UserControl
    x:Class="Dash.MenuToolbar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrl="using:Windows.UI.Xaml.Controls"
    xmlns:userControl="using:RadialMenuControl.UserControl"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="800">

    <!--Stackpanel containing toolbars wrapped in an instance of floating, which manages all dragging and repositioning events externally-->
    <userControl:Floating x:Name="Floating" IsBoundByParent="False" IsBoundByScreen="True">
        <StackPanel>
            <ctrl:CommandBar x:Name="xToolbar" IsOpen="True">
                <AppBarButton Icon="TouchPointer" Label="Pointer" />
                <AppBarButton Icon="Edit" Label="Ink" />
                <AppBarButton Icon="SelectAll" Label="Group Tool" />
                <AppBarSeparator />
                <AppBarButton Icon="Rename" Label="Add Note" />
                <AppBarButton Icon="ViewAll" Label="Add Group" />
                <AppBarButton Icon="Pictures" Label="Add Image" Tapped="AddImage_OnTapped" />
                <AppBarButton Icon="Video" Label="Add Video" Click="Add_Video_On_Click" />
                <AppBarSeparator />
                <AppBarButton Icon="Copy" x:Name="xCopy" Label="Duplicate" Click="Copy" />
                <AppBarButton Icon="Delete" x:Name="xDelete" Label="Delete" Click="Delete" />
            </ctrl:CommandBar>
            <StackPanel Margin="0,12,0,0">
                <local:TextSubtoolbar x:Name="xTextToolbar" Visibility="Collapsed" />
                <local:ImageSubtoolbar Visibility="Collapsed" x:Name="xImageToolbar" />
                <local:CollectionSubtoolbar Visibility="Collapsed" x:Name="xCollectionToolbar" />
            </StackPanel>
        </StackPanel>
    </userControl:Floating>

</UserControl>















