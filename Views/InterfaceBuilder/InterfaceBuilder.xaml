<local:WindowTemplate
    x:Class="Dash.InterfaceBuilder"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    MinWidth="800"
    MinHeight="100">

    <local:WindowTemplate.InnerContent>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListView x:Name ="BreadcrumbListView" HorizontalAlignment="Stretch" VerticalAlignment="Top"
                      ItemClick="BreadcrumbListView_ItemClick" IsItemClickEnabled="True" Grid.Row="0" >
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsStackPanel Orientation="Horizontal"></ItemsStackPanel>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Path=DocumentType}"></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{StaticResource WindowsBlue}" Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <local:KeyValuePane x:Name="xKeyValuePane" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="300" MinWidth="300"></local:KeyValuePane>

                <Grid x:Name="xDocumentPane" Grid.Column="1" Margin="1,1" Background="White">
                    <ScrollViewer  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" ZoomMode="Enabled" MaxZoomFactor="4"
                              MinZoomFactor="1">
                        <Viewbox x:Name="xDocumentHolder" Stretch="Uniform" MaxWidth="{Binding Path=ActualWidth, ElementName=xDocumentPane}" MaxHeight="{Binding Path=ActualHeight, ElementName=xDocumentPane}"/>
                    </ScrollViewer>
                </Grid>

                <Grid x:Name="xSettingsPane" Width="300" MinWidth="300" Height="Auto" HorizontalAlignment="Center" VerticalAlignment="Stretch" Grid.Column="2"
                  Background="White" BorderBrush="{StaticResource WindowsBlue}" BorderThickness="1,0,0,0" />
            </Grid>

            <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center">
                <Button x:Name="ListButton" Content="🖹" Width="45" Height="45" Margin="5"  VerticalContentAlignment="Center" FontSize="25" Tapped="List_Tapped" ></Button>
                <Button x:Name="GridButton" Content="▦" Width="45" Height="45" Margin="5" VerticalContentAlignment="Center" FontSize="25" Tapped="Grid_Tapped"></Button>
                <Button x:Name="FreeformButton" Content="⊡" Width="45" Height="45" Margin="5" VerticalContentAlignment="Center" FontSize="25" Tapped="Freeform_Tapped" ></Button>
            </StackPanel>
        </Grid>
    </local:WindowTemplate.InnerContent>
</local:WindowTemplate>