<UserControl
    x:Class="Dash.DocumentSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Dash"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="230">

    <Grid HorizontalAlignment="Stretch" Height="Auto" BorderBrush="Transparent">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--EDIT-->
            <Grid Grid.Row="0" Width="230" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Button Margin="0,5,0,10" Height="35" HorizontalAlignment="Stretch" Content="EDIT">
                    <Button.Background>
                        <SolidColorBrush Color="SteelBlue"></SolidColorBrush>
                    </Button.Background>
                </Button>
            </Grid>

            <!--LAYOUT-->
            <Grid Grid.Row="1" Width="230" HorizontalAlignment="Left" VerticalAlignment="Center">
                <local:SettingsPaneBlock>
                    <local:SettingsPaneBlock.MainContent>
                        <Grid Margin="0,5,0,5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                            </Grid.RowDefinitions>
                            <!--COMBO BOX-->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="4.5*" />
                                    <ColumnDefinition Width="1*" />
                                </Grid.ColumnDefinitions>
                                <ComboBox x:Name="xActiveLayoutComboBox" HorizontalAlignment="Stretch"
                                          BorderBrush="Transparent" FontSize="12"
                                          SelectionChanged="XActiveLayoutComboBox_OnSelectionChanged">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding LayoutName}"></TextBlock>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                                
                                <!--NEW LAYOUT OPTIONS DROPDOWN--> 
                                <ComboBox x:Name="xAddLayoutComboBox" HorizontalAlignment="Stretch" Grid.Column="1" Visibility="Collapsed" MinWidth="36"
                                          BorderBrush="Transparent" FontSize="12" IsDropDownOpen="True">
                                </ComboBox>

                                <!--ADD LAYOUT BUTTON-->
                                <Button x:Name="xAddLayoutButton" Background="SteelBlue" Grid.Column="1" >
                                    <SymbolIcon Symbol="Add"></SymbolIcon>
                                </Button>
                            </Grid>
                        </Grid>
                    </local:SettingsPaneBlock.MainContent>
                    <local:SettingsPaneBlock.Title>LAYOUT</local:SettingsPaneBlock.Title>
                </local:SettingsPaneBlock>
            </Grid>

            
            
        </Grid>
    </Grid>
</UserControl>































































