<UserControl
    x:Class="Dash.CollectionDBSchemaHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PointerPressed="UserControl_PointerPressed" 
    ManipulationMode="All"
    ManipulationDelta="UserControl_ManipulationDelta"
    ManipulationStarted="UserControl_ManipulationStarted"
    DragStarting="UserControl_DragStarting"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <TextBlock FontSize="12" Foreground="Black" Text="{Binding}" FontWeight="{Binding Selected,Converter={StaticResource BoolToBoldConverter},Mode=OneWay}" 
               Width="{Binding Width}" Height="25" Tapped="SelectTap" DoubleTapped="TextBlock_DoubleTapped"     
               />

</UserControl>
