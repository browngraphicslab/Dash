<UserControl
    x:Class="Dash.Views.CollectionDBSchemaHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <TextBlock FontSize="12" Foreground="Black" Text="{Binding}" FontWeight="{Binding Selected,Converter={StaticResource BoolToBoldConverter},Mode=OneWay}" Width="{Binding Width}" Height="25" Tapped="SelectTap" DoubleTapped="TextBlock_DoubleTapped"     />

</UserControl>
