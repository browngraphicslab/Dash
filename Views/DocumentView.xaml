<UserControl
    x:Class="Dash.DocumentView"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Dash"
    xmlns:views="using:Dash.Views"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance local:DocumentViewModel}"
    d:DesignWidth="400"
    d:DesignHeight="400"
    AllowDrop="True"
    DragOver="This_DragOver"
    DragLeave="This_DragLeave"
    Drop="This_Drop"
    UseLayoutRounding="False">
    <UserControl.Resources>
        <Storyboard x:Name="FadeOut" Completed="FadeOut_Completed" SpeedRatio="0.7">
            <FadeOutThemeAnimation TargetName="Root"></FadeOutThemeAnimation>
        </Storyboard>
        <local:BoolToBrushConverter x:Key="BoolToBrushConverter"/>
        <SolidColorBrush x:Key="DocumentBackground" Color="{ThemeResource DocumentBackgroundColor}"></SolidColorBrush>
    </UserControl.Resources>

    <Grid x:Name="xMasterStack" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" x:Name="xAnnotationColumn"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition  Height="Auto"/>
            <RowDefinition  Height="*"/>
        </Grid.RowDefinitions>

        <!--Key Value Flyout-->
        
        <Border
            x:Name="xKeyValueBorder"
            Grid.Column="0"
            Grid.Row="0"
            Height="0"
            Visibility="Visible">
            <Border.Resources>
                <Storyboard x:Name="xQuickEntryIn">
                    <DoubleAnimation 
                        Storyboard.TargetName="xKeyValueBorder"
                        Storyboard.TargetProperty="MinHeight"
                        To="60"
                        Duration="0:0:0.2"
                        EnableDependentAnimation="True"/>
                </Storyboard>
                <Storyboard x:Name="xQuickEntryOut">
                    <DoubleAnimation 
                        Storyboard.TargetName="xKeyValueBorder"
                        Storyboard.TargetProperty="MinHeight"
                        To="0"
                        Duration="0:0:0.2"
                        EnableDependentAnimation="True"/>
                </Storyboard>
                <Storyboard x:Name="xKeyEditFailure">
                    <ColorAnimation 
                        Storyboard.TargetName="xKeyBox"
                        Storyboard.TargetProperty="(TextBox.Background).(SolidColorBrush.Color)"
                        To="Red"
                        AutoReverse="True"
                        Duration="0:0:0.2"
                        EnableDependentAnimation="True"/>
                </Storyboard>
                <Storyboard x:Name="xValueEditFailure">
                    <ColorAnimation 
                        Storyboard.TargetName="xValueBox"
                        Storyboard.TargetProperty="(TextBox.Background).(SolidColorBrush.Color)"
                        To="Red"
                        AutoReverse="True"
                        Duration="0:0:0.2"
                        EnableDependentAnimation="True"/>
                </Storyboard>
                <Storyboard x:Name="xValueErrorFailure">
                    <ColorAnimation 
                        Storyboard.TargetName="xValueBox"
                        Storyboard.TargetProperty="(TextBox.Background).(SolidColorBrush.Color)"
                        To="Navy"
                        AutoReverse="True"
                        Duration="0:0:0.2"
                        EnableDependentAnimation="True"/>
                </Storyboard>
                <Storyboard x:Name="xKeyEditSuccess">
                    <ColorAnimation 
                        Storyboard.TargetName="xKeyBox"
                        Storyboard.TargetProperty="(TextBox.Background).(SolidColorBrush.Color)"
                        To="ForestGreen"
                        AutoReverse="True"
                        Duration="0:0:0.2"
                        EnableDependentAnimation="True"/>
                </Storyboard>
                <Storyboard x:Name="xValueEditSuccess">
                        <ColorAnimation 
                        Storyboard.TargetName="xValueBox"
                        Storyboard.TargetProperty="(TextBox.Background).(SolidColorBrush.Color)"
                        To="ForestGreen"
                        AutoReverse="True"
                        Duration="0:0:0.2"
                        EnableDependentAnimation="True"/>
                </Storyboard>
            </Border.Resources>
            <Grid>
                <!--<Rectangle
                    Fill="Gray"
                    RadiusX="5"
                    RadiusY="5"
                    Opacity="0.5"
                    Margin="0, 0, 0, 0"/>-->
                <Grid Margin="0, 0, 0, 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <local:ActionTextBox
                        x:Name="xKeyBox"
                        Margin="7, 0, 3.5, 0"
                        Padding="7, 3, 0, 0"
                        MaxLength="20"
                        Grid.Column="0"
                        Width="Auto"
                        AcceptsReturn="False"
                        IsSpellCheckEnabled="False"
                        Height="25"/>
                    <local:ActionTextBox
                        x:Name="xValueBox"
                        Margin="3.5, 0, 7, 0"
                        Padding="7, 3, 0, 0"
                        Grid.Column="1"
                        Width="Auto"
                        AcceptsReturn="False"
                        IsSpellCheckEnabled="False"
                        PlaceholderText="Value..."
                        Height="25"/>
                </Grid>
            </Grid>

        </Border>

        <!-- border to highlight search results-->
        <Border 
            Name="DocHighlight" 
            x:FieldModifier="public" 
            BorderBrush="#fffc84"
            BorderThickness="{Binding SearchHighlightState}" 
            Grid.Row="1" 
            Grid.Column="0">
            <Border x:Name="xTargetBorder">
                <Grid x:Name="LayoutRoot" x:FieldModifier="public" BorderThickness="0" Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition x:Name="xLeftColumn" Width="15" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition x:Name="xRightColumn" Width="15" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition x:Name="xTopRow" Height="15" />
                        <RowDefinition Height="*" />
                        <RowDefinition x:Name="xBottomRow" Height="15" />
                    </Grid.RowDefinitions>

                    <!--Main document view 
 
                     Context flyout for right-click context menu; this opens automatically when the DocumentView grid is right clicked and is dismissed when anything else is clicked-->
                    <Grid.ContextFlyout>
                        <MenuFlyout x:Name="xMenuFlyout">

                            <!--There's a MenuFlyoutItem for each clickable item in the right click menu for DocumentViews; CollectionViews will add more items to this-->

                            <MenuFlyoutItem Text="Open" Icon="OpenFile" Click="MenuFlyoutItemOpen_OnClick" />
                            <MenuFlyoutItem Text="Delete" Icon="Delete" Click="MenuFlyoutItemDelete_Click" />
                            <MenuFlyoutSeparator />
                            <MenuFlyoutItem Text="Duplicate" Icon="Copy" Click="MenuFlyoutItemCopy_Click" />
                            <MenuFlyoutItem Text="Alias" Icon="TwoPage" Click="MenuFlyoutItemAlias_Click" />
                            <MenuFlyoutSeparator />
                            <MenuFlyoutItem Text="Fields" Icon="ShowResults" Click="MenuFlyoutItemFields_Click" />
                            <MenuFlyoutItem Text="Context" Icon="SetLockScreen" Click="MenuFlyoutItemContext_Click" />
                            <MenuFlyoutSeparator />

                            <MenuFlyoutItem Text="Pin to Presentation" Icon="Pin" Click="MenuFlyoutItemPin_Click" />
                            <MenuFlyoutItem Text="Apply Template" Icon="MoveToFolder"
                                            Click="MenuFlyoutItemApplyTemplate_Click" />
                            <MenuFlyoutItem Text="Toggle As Adornment" Icon="Trim"
                                            Click="MenuFlyoutItemToggleAsAdornment_Click" />
                            <MenuFlyoutItem Text="Screen Capture" Icon="WebCam" Click="MenuFlyoutItemScreenCap_Click" />
                            <MenuFlyoutItem Text="Launch App" Icon="Go" Click="MenuFlyoutLaunch_Click" />
                            <MenuFlyoutItem Text="Copy History to Clipboard" Icon="Paste"
                                            Click="MenuFlyoutItemCopyHistory_Click" />

                        </MenuFlyout>
                    </Grid.ContextFlyout>

                    <!--New resize handler controls-->

                    <Rectangle x:Name="xTopLeftResizeControl" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                               Grid.Row="0" PointerExited="AllResizers_PointerExited"
                               Grid.Column="0" x:FieldModifier="public" ManipulationMode="TranslateY, TranslateX"
                               PointerEntered="NWSE_Direction_PointerEntered" Fill="Transparent" />
                    <Rectangle x:Name="xTopResizeControl" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                               Grid.Row="0" PointerExited="AllResizers_PointerExited"
                               Grid.Column="1" x:FieldModifier="public" ManipulationMode="TranslateY"
                               PointerEntered="Y_Direction_PointerEntered" Fill="Transparent" />
                    <Rectangle x:Name="xTopRightResizeControl" HorizontalAlignment="Stretch"
                               VerticalAlignment="Stretch" Grid.Row="0" PointerExited="AllResizers_PointerExited"
                               Grid.Column="2" x:FieldModifier="public" ManipulationMode="TranslateY, TranslateX"
                               PointerEntered="NESW_Direction_PointerEntered" Fill="Transparent" />
                    <Rectangle x:Name="xLeftResizeControl" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                               Grid.Row="1" PointerExited="AllResizers_PointerExited"
                               Grid.Column="0" x:FieldModifier="public" ManipulationMode="TranslateX"
                               PointerEntered="X_Direction_PointerEntered" Fill="Transparent" />
                    <Rectangle x:Name="xRightResizeControl" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                               Grid.Row="1" PointerExited="AllResizers_PointerExited"
                               Grid.Column="2" x:FieldModifier="public" ManipulationMode="TranslateX"
                               PointerEntered="X_Direction_PointerEntered" Fill="Transparent" />
                    <Rectangle x:Name="xBottomLeftResizeControl" HorizontalAlignment="Stretch"
                               VerticalAlignment="Stretch" Grid.Row="2" PointerExited="AllResizers_PointerExited"
                               Grid.Column="0" x:FieldModifier="public" ManipulationMode="TranslateY, TranslateX"
                               PointerEntered="NESW_Direction_PointerEntered" Fill="Transparent" />
                    <Rectangle x:Name="xBottomResizeControl" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                               Grid.Row="2" PointerExited="AllResizers_PointerExited"
                               Grid.Column="1" x:FieldModifier="public" ManipulationMode="TranslateY"
                               PointerEntered="Y_Direction_PointerEntered" Fill="Transparent" />
                    <Rectangle x:Name="xBottomRightResizeControl" HorizontalAlignment="Stretch"
                               VerticalAlignment="Stretch" Grid.Row="2" PointerExited="AllResizers_PointerExited"
                               Grid.Column="2" x:FieldModifier="public" ManipulationMode="TranslateY, TranslateX"
                               PointerEntered="NWSE_Direction_PointerEntered" Fill="Transparent" />

                    <Grid Grid.Row="1" Grid.Column="1" x:Name="xContentGrid">

                        <!--Used to render the drop shadow and provide a background to the document-->

                        <Canvas x:Name="xShadowHost" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                        <Rectangle x:Name="xDocumentBackground" HorizontalAlignment="Stretch"
                                   VerticalAlignment="Stretch" Fill="Transparent" />


                        <!--TODO this relative panel no longer needs to exist its original reason for existing is gone


                     border to allow document to be highlighted when selected
                     Container for document fields view-->

                        <Grid x:Name="MainContent">
                            <ContentPresenter x:Name="xContentPresenter" x:FieldModifier="public"
                                              Content="{Binding Content}" />
                            <Grid x:Name="xIcon" Visibility="Collapsed" HorizontalAlignment="Center"
                                  VerticalAlignment="Center">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"></RowDefinition>
                                    <RowDefinition Height="Auto"></RowDefinition>
                                </Grid.RowDefinitions>

                                <Border x:Name="xIconBorder" BorderBrush="DarkGray" Grid.Row="0" Grid.Column="0">
                                    <Grid>
                                        <Image x:Name="xIconImage" Height="100" HorizontalAlignment="Center"
                                               Source="ms-appx:///Assets/Icons/doc-icon.png">
                                            <ToolTipService.ToolTip>
                                                <Image x:Name="xToolTipPreview" Height="200"
                                                       Source="ms-appx:///Assets/Icons/Unavailable.png">
                                                </Image>
                                            </ToolTipService.ToolTip>
                                        </Image>
                                        <Image x:Name="xSmallIconImage" Height="35" Width="35"
                                               HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                               Source="ms-appx:///Assets/Icons/doc-icon.png" />
                                    </Grid>
                                </Border>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="1">
                                    <TextBox x:Name="xIconLabel" FontSize="25" MaxWidth="86" MinHeight="5"
                                             Padding="1,4,0,0" TextWrapping="Wrap" HorizontalAlignment="Center"
                                             TextAlignment="Center" Background="Transparent"
                                             Text="{Binding DocumentController.Title}" BorderThickness="0" />
                                </StackPanel>
                            </Grid>
                        </Grid>


                        <Canvas x:Name="xContextCanvas" HorizontalAlignment="Left" VerticalAlignment="Top">
                            <HyperlinkButton
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                Foreground="CornflowerBlue"
                                Content=""
                                VerticalContentAlignment="Center"
                                x:Name="xContextTitle"
                                Margin="0, 0, 15, 0"
                                Visibility="Collapsed" />
                        </Canvas>
                    </Grid>

                </Grid>
            </Border>
        </Border>

        <Border 
            x:Name="xTitleBorder" 
            Grid.Column="0" 
            Grid.Row="1"
            RelativePanel.AlignRightWithPanel="True"
            Opacity="{Binding DecorationState, Converter={StaticResource BoolToOpacityConverter}}"
            Background="Transparent" 
            HorizontalAlignment="Left"
            Margin="-32, 0, 0, 0" 
            VerticalAlignment="Top">
            <TextBlock 
                x:Name="xTitleIcon"
                FontSize="16"
                FontFamily="{StaticResource FontAwesome}"
                Foreground="{StaticResource TitleText}"
                Text="{StaticResource DocumentPlainIcon}"
                HorizontalAlignment="Stretch"
                Padding="0, 0, 10, 0"
                Tapped="XTitleIcon_OnTapped"/>
        </Border>

        <Border 
            x:Name="xOperatorEllipseBorder"
            Grid.Column="1"
            Grid.Row="1"
            CanDrag="True"
            Background="Transparent"
            Opacity="{Binding DecorationState, Converter={StaticResource BoolToOpacityConverter}}"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Padding="5, 0, 0, 0">
            <Grid>
                <Ellipse
                    x:Name="OperatorEllipseUnhighlight"
                    Style="{StaticResource xFieldHandle}"
                    Width="25"
                    Height="25" />
                <Ellipse
                    x:Name="OperatorEllipseHighlight"
                    Style="{StaticResource xFieldHandleHighlighted}"
                    Width="25"
                    Height="25"
                    Visibility="Collapsed" />
                <TextBlock
                    x:Name="xOperatorEllipseText"
                    Foreground="White"
                    FontFamily="Segoe MDL2 Assets"
                    Text="&#xE11B;"
                    FontSize="12"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="1,0,0,0" />
                <ToolTipService.ToolTip>
                    <ToolTip Content="Make Copy" Placement="Right" HorizontalOffset="40" VerticalOffset="0" />
                </ToolTipService.ToolTip>
            </Grid>
        </Border>

        <Border 
            x:Name="xAnnotateEllipseBorder" 
            Grid.Column="1" 
            Grid.Row="1" 
            CanDrag="True" 
            Background="Transparent"
            Opacity="{Binding DecorationState, Converter={StaticResource BoolToOpacityConverter}}"
            HorizontalAlignment="Left"
            VerticalAlignment="Top" 
            Padding="5 0 0 0"
            Tapped="XAnnotateEllipseBorder_OnTapped_">
            <Grid>
                <Ellipse 
                    x:Name="AnnotateEllipseUnhighlight" 
                    Style="{StaticResource xFieldLinkHandle}" 
                    Width="25"
                    Height="25" />
                <Ellipse 
                    x:Name="AnnotateEllipseHighlight" 
                    Style="{StaticResource xFieldLinkHandleHighlighted}"
                    Visibility="Collapsed" 
                    Width="25" 
                    Height="25" />
                <TextBlock 
                    x:Name="xAnnotateEllipseText" 
                    Foreground="White" 
                    FontFamily="Segoe MDL2 Assets"
                    Text="&#xE167;" 
                    FontSize="12" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Margin="0,0,0,0" />
                <ToolTipService.ToolTip>
                    <ToolTip 
                        Content="Create A Link" 
                        Placement="Right" 
                        HorizontalOffset="40" 
                        VerticalOffset="0" />
                </ToolTipService.ToolTip>
            </Grid>
        </Border>

        <Border 
            x:Name="xTemplateEditorEllipseBorder" 
            Grid.Column="1"
            Grid.Row="1" 
            CanDrag="True"
            Background="Transparent"
            Opacity="{Binding DecorationState, Converter={StaticResource BoolToOpacityConverter}}"
            HorizontalAlignment="Left" 
            VerticalAlignment="Top" 
            Padding="5 0 0 0">
            <Grid HorizontalAlignment="Left">
                <Ellipse
                    x:Name="TemplateEditorEllipseBorderUnhighlight" Style="{StaticResource xFieldTemplateHandle}"
                    Width="25" Height="25" />
                <Ellipse 
                    x:Name="TemplateEditorEllipseBorderHighlight"
                    Style="{StaticResource xFieldTemplateHandleHighlighted}" Width="25" Height="25"
                    Visibility="Collapsed"/>
                <TextBlock 
                    x:Name="xTemplateEllipseText" 
                    Foreground="White" 
                    FontFamily="Segoe MDL2 Assets"
                    Text="&#xE14E;" 
                    FontSize="12" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Margin="1,0,0,0" />
                <ToolTipService.ToolTip>
                    <ToolTip Content="Open Template Editor" Placement="Right" HorizontalOffset="40" VerticalOffset="0" />
                </ToolTipService.ToolTip>
            </Grid>
        </Border>

    </Grid>
          


</UserControl>