<dash:SelectionElement
    x:Class="Dash.InkCanvasControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dash="using:Dash"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" ManipulationMode="None" SizeChanged="SelectionElement_SizeChanged"
    MaxWidth="1000"
    MaxHeight="1000">
    <Grid x:Name="Grid" SizeChanged="Grid_SizeChanged" BorderThickness="1">
        <ScrollViewer x:Name="ScrollViewer" VerticalScrollMode="Enabled" HorizontalScrollMode="Enabled"
                      VerticalScrollBarVisibility="Auto" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                      HorizontalScrollBarVisibility="Auto"
                      ZoomMode="Enabled" MinZoomFactor=".1" MaxZoomFactor="4">
            <Grid Height="2000" Width="2000">
                <InkCanvas x:Name="XInkCanvas" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"></InkCanvas>
            </Grid>
        </ScrollViewer>
        <SymbolIcon x:Name="EditingSymbol" Symbol="Edit" Width="50" Height="50" VerticalAlignment="Top"
                    HorizontalAlignment="Right" Foreground="LightGray" />
        <Grid.Clip>
            <RectangleGeometry x:Name="ClipRect" Rect="0,0,400,300"></RectangleGeometry>
        </Grid.Clip>
    </Grid>
</dash:SelectionElement>

