<UserControl
    x:Class="Dash.PDFRegionMarker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="xUp" Height="*"/>
            <RowDefinition x:Name="xRegionRow" Height="10"/>
            <RowDefinition x:Name="xDown" Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="xLeft" Width="*"/>
            <ColumnDefinition x:Name="xRegionColumn" Width="*"/>
            <ColumnDefinition x:Name="xRight" Width="*"/>
        </Grid.ColumnDefinitions>
        <Rectangle x:Name="xRegion" Grid.Row="1" Grid.Column="1" Fill="Gold"/>
    </Grid>
</UserControl>