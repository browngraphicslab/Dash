<UserControl
    x:Class="Dash.ShapeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" d:DataContext="{d:DesignInstance local:ShapeViewModel}"
    Canvas.Left="{Binding X, Mode=TwoWay}" Canvas.Top="{Binding Y, Mode=TwoWay}">

    <Rectangle Fill="HotPink" Width="{Binding Width}" Height="{Binding Height}"
               ManipulationMode="All" ManipulationDelta="ShapeOnManipulationDelta">

    </Rectangle>
</UserControl>
