<UserControl
    x:Class="Dash.KVPRow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name ="xRow" >
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition Width="5*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid x:Name="xKey" Column="0"  >
            <TextBlock FontWeight="SemiBold" Text="{Binding Key}"></TextBlock>
        </Grid>

        <Grid x:Name="xValue"  Column="1" BorderBrush="DarkGray" BorderThickness="0.5 0 0 0" >
            <ContentPresenter x:Name="XValuePresenter" HorizontalAlignment="Center"></ContentPresenter>
        </Grid>

    </Grid>
</UserControl>
