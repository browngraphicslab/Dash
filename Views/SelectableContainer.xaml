<UserControl
    x:Class="Dash.SelectableContainer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid x:Name="XGrid" BorderBrush="{StaticResource WindowsBlue}" Background="White" Margin="18,18,18,18" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <ContentControl x:Name="XLayoutDisplay" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"></ContentControl>
        </Grid>
        <Ellipse x:Name="xBottomLeftDragger"  HorizontalAlignment="Left" VerticalAlignment="Bottom" Style="{StaticResource xDraggerEllipseStyle}"/>
        <Ellipse x:Name="xBottomRightDragger" HorizontalAlignment="Right" VerticalAlignment="Bottom" Style="{StaticResource xDraggerEllipseStyle}"/>
        <Ellipse x:Name="xTopLeftDragger" HorizontalAlignment="Left" VerticalAlignment="Top" Style="{StaticResource xDraggerEllipseStyle}"/>
        <Ellipse x:Name="xTopRightDragger" HorizontalAlignment="Right" VerticalAlignment="Top" Style="{StaticResource xDraggerEllipseStyle}" />
        <Ellipse x:Name="xCenterDragger" HorizontalAlignment="Center"  VerticalAlignment="Center" Style="{StaticResource xDraggerEllipseStyle}"/>
    </Grid>

</UserControl>
