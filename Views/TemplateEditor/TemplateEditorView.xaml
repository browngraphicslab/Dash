<UserControl
    x:Class="Dash.TemplateEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:media="using:Syncfusion.UI.Xaml.Controls.Media"
    mc:Ignorable="d"
    x:Name="xEditorControl"
    AllowDrop="True"
    d:DesignHeight="500"
    d:DesignWidth="1000">

    <StackPanel Orientation="Horizontal" BorderBrush="Black" BorderThickness="2" x:Name="xOuterPanel">
        <StackPanel Orientation="Vertical" x:Name="xDataPanel" Width="300" Background="White" />

        <Canvas VerticalAlignment="Top" Width="500" Height="500" Background="LightSlateGray"  HorizontalAlignment="Left">
            <Canvas VerticalAlignment="Top" Width="300" Height="400" Background="White" HorizontalAlignment="Left"
                    Canvas.Left="100" Canvas.Top="50" x:FieldModifier="public"
                    x:Name="xWorkspace" Loaded="XWorkspace_OnLoaded" Unloaded="XWorkspace_OnUnloaded" Drop="XWorkspace_OnDrop" AllowDrop="True">
                <ItemsControl x:Name="xItemsControl"
                              x:FieldModifier="public"
                              ItemsSource="{Binding DocumentViewModels}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas x:Name="xCollectionCanvas"
                                    x:FieldModifier="public"
                                    Background="Transparent"
                                    Width="0"
                                    Height="0"
                                    VerticalAlignment="Top"
                                    HorizontalAlignment="Left" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:DocumentView
                                BindRenderTransform="True"
                                Loaded="DocumentView_OnLoaded"
                                SizeChanged="FrameworkElement_OnSizeChanged"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Canvas>
        </Canvas>

        <StackPanel Orientation="Vertical" x:Name="xLayoutPanel" Width="200" Background="DodgerBlue" Padding="10 10 10 10">
            <TextBlock FontSize="16" FontWeight="Bold" HorizontalAlignment="Right" Foreground="Black">Data Key</TextBlock>
            <local:EditableTextBlock Width="180" FontSize="16" HorizontalAlignment="Left" x:Name="xKeyBox"></local:EditableTextBlock>
            <TextBlock FontSize="16" FontWeight="Bold" HorizontalAlignment="Right" Foreground="Black">Add Item</TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <AppBarButton Icon="Rename" Height="50" Width="50" Click="TextButton_OnClick"></AppBarButton>
                <AppBarButton Icon="Pictures" Height="50" Width="50" Click="ImageButton_OnClick"></AppBarButton>
                <AppBarButton Icon="Video" Height="50" Width="50" Click="VideoButton_OnClick"></AppBarButton>
                <AppBarButton Icon="Audio" Height="50" Width="50" Click="AudioButton_OnClick"></AppBarButton>
            </StackPanel>

            <TextBlock FontSize="16" FontWeight="Bold" HorizontalAlignment="Right" Foreground="Black">Horizontal Alignment</TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <AppBarButton Icon="AlignLeft" x:Name="xAlignLeftButton" Height="50" Width="60" Click="AlignmentButton_OnChecked" ></AppBarButton>
                <AppBarButton Icon="AlignCenter" x:Name="xAlignCenterButton" Height="50" Width="60" Click="AlignmentButton_OnChecked"></AppBarButton>
                <AppBarButton Icon="AlignRight" x:Name="xAlignRightButton" Height="50" Width="60" Click="AlignmentButton_OnChecked" ></AppBarButton>
            </StackPanel>

            <!--<TextBlock FontSize="16" FontWeight="Bold" HorizontalAlignment="Right" Foreground="Black">Border</TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <AppBarToggleButton Icon="Import" Height="50" Width="50" x:Name="xLeftBorderChecker" Checked="BorderOption_OnChanged" Unchecked="BorderOption_OnChanged"></AppBarToggleButton>
                <AppBarToggleButton Icon="Up" Height="50" Width="50" x:Name="xTopBorderChecker" Checked="BorderOption_OnChanged" Unchecked="BorderOption_OnChanged"></AppBarToggleButton>
                <AppBarToggleButton Icon="Forward" Height="50" Width="50" x:Name="xRightBorderChecker" Checked="BorderOption_OnChanged" Unchecked="BorderOption_OnChanged"></AppBarToggleButton>
                <AppBarToggleButton Icon="Download" Height="50" Width="50" x:Name="xBottomBorderChecker" Checked="BorderOption_OnChanged" Unchecked="BorderOption_OnChanged"></AppBarToggleButton>
            </StackPanel>

            <Slider x:Name="xThicknessSlider" Header="Border Thickness" Width="200" Maximum="10"
                    ValueChanged="XThicknessSlider_OnValueChanged" />
            <media:SfColorPicker x:Name="xColorPicker" PointerReleased="XColorPicker_OnPointerReleased"/>-->

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <AppBarButton Icon="Undo"  x:Name="xUndoButton" Height="50" Width="50" Click="XUndoButton_OnClick" ></AppBarButton>
                <AppBarButton Icon="Redo"  x:Name="xRedoButton" Height="50" Width="50" Click="XRedoButton_OnClick" ></AppBarButton>
                <AppBarButton Icon="DisableUpdates"  x:Name="xResetButton" Height="50" Width="50" Click="XResetButton_OnClick" ></AppBarButton>
                <AppBarButton Icon="Clear"  x:Name="xClearButton" Height="50" Width="50" Click="XClearButton_OnClick" ></AppBarButton>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Height="50" Width="180" Background="DodgerBlue" Click="XUploadTemplate_OnClick">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontWeight="Bold" FontSize="16" Foreground="Black">Upload Template</TextBlock>
                        <TextBlock FontSize="26" Foreground="Black"
                                   FontFamily="{StaticResource FontAwesome}" Text="{StaticResource DocumentIcon}"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Height="50" Width="180" Background="DodgerBlue" Click="ApplyChanges_OnClicked">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontWeight="Bold" FontSize="16" Foreground="Black">Apply Changes</TextBlock>
                        <TextBlock FontSize="26" Foreground="Black"
                                   FontFamily="{StaticResource FontAwesome}" Text="{StaticResource SaveIcon}"/>
                    </StackPanel>
                </Button>
            </StackPanel>
            

        </StackPanel>
    </StackPanel>
</UserControl>
