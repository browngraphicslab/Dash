<UserControl
    x:Class="Dash.TemplateEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="xEditorControl"
    AllowDrop="True"
    d:DesignHeight="300"
    d:DesignWidth="1000">

    <StackPanel Orientation="Horizontal">
        <StackPanel Orientation="Vertical" x:Name="xDataPanel" Width="300" Background="White">
           
        </StackPanel>

        <Canvas VerticalAlignment="Top" Width="500" Height="500" Background="White"  HorizontalAlignment="Left"
                x:Name="xWorkspace" Loaded="XWorkspace_OnLoaded" Unloaded="XWorkspace_OnUnloaded">
            <ItemsControl x:Name="xItemsControl"
                          x:FieldModifier="public"
                          ItemsSource="{Binding DocumentViewModels}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas x:Name="xCollectionCanvas"
                                x:FieldModifier="public"
                                Background="Transparent"
                                Width="0"
                                Height="0"
                                VerticalAlignment="Top"
                                HorizontalAlignment="Left"
                                RenderTransform="{Binding TransformGroup, Converter={StaticResource TransformGroupDataToGroupTransformConverter}}"
                        >
                        </Canvas>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:DocumentView
                                BindRenderTransform="True" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>

        <StackPanel Orientation="Vertical" x:Name="xLayoutPanel" Width="200" Background="DodgerBlue" Padding="10 10 10 10">
            <TextBlock FontSize="16" FontWeight="Bold" HorizontalAlignment="Right">Add Item</TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <AppBarButton Icon="Rename" Height="50" Width="50" Click="TextButton_OnClick"></AppBarButton>
                <AppBarButton Icon="Pictures" Height="50" Width="50" Click="ImageButton_OnClick"></AppBarButton>
                <AppBarButton Icon="Video" Height="50" Width="50" Click="VideoButton_OnClick"></AppBarButton>
                <AppBarButton Icon="Audio" Height="50" Width="50" Click="AudioButton_OnClick"></AppBarButton>
            </StackPanel>
            <TextBlock FontSize="16" FontWeight="Bold" HorizontalAlignment="Right">Horizontal Alignment</TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <AppBarToggleButton Icon="AlignLeft" Height="50" Width="60" Checked="ToggleButton_OnChecked" Unchecked="ToggleButton_OnUnchecked"></AppBarToggleButton>
                <AppBarToggleButton Icon="AlignCenter" Height="50" Width="60" Checked="ToggleButton_OnChecked" Unchecked="ToggleButton_OnUnchecked"></AppBarToggleButton>
                <AppBarToggleButton Icon="AlignRight" Height="50" Width="60" Checked="ToggleButton_OnChecked" Unchecked="ToggleButton_OnUnchecked"></AppBarToggleButton>
            </StackPanel>
            <TextBlock FontSize="16" FontWeight="Bold" HorizontalAlignment="Right">Border</TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <AppBarToggleButton Icon="Import" Height="50" Width="50" Checked="LeftBorder_OnChecked" Unchecked="LeftBorder_OnUnchecked"></AppBarToggleButton>
                <AppBarToggleButton Icon="Up" Height="50" Width="50" Checked="TopBorder_OnChecked" Unchecked="TopBorder_OnUnchecked"></AppBarToggleButton>
                <AppBarToggleButton Icon="Forward" Height="50" Width="50" Checked="RightBorder_OnChecked" Unchecked="RightBorder_OnUnchecked"></AppBarToggleButton>
                <AppBarToggleButton Icon="Download" Height="50" Width="50" Checked="BottomBorder_OnChecked" Unchecked="BottomBorder_OnUnchecked"></AppBarToggleButton>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>
