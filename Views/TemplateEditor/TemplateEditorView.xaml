<UserControl
    x:Class="Dash.TemplateEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="xEditorControl"
    AllowDrop="True"
    d:DesignHeight="300"
    d:DesignWidth="1000">

    <StackPanel Orientation="Horizontal">
        <StackPanel Orientation="Vertical" x:Name="xDataPanel" Width="300" Background="White">
           
        </StackPanel>

        <Canvas VerticalAlignment="Top" Width="500" Height="500" Background="White"  HorizontalAlignment="Left"
                x:Name="xWorkspace" Loaded="XWorkspace_OnLoaded" Unloaded="XWorkspace_OnUnloaded">
            <ItemsControl x:Name="xItemsControl"
                          x:FieldModifier="public"
                          ItemsSource="{Binding DocumentViewModels}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas x:Name="xCollectionCanvas"
                                x:FieldModifier="public"
                                Background="Transparent"
                                Width="0"
                                Height="0"
                                VerticalAlignment="Top"
                                HorizontalAlignment="Left"
                                RenderTransform="{Binding TransformGroup, Converter={StaticResource TransformGroupDataToGroupTransformConverter}}"
                        >
                        </Canvas>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:DocumentView
                                BindRenderTransform="True" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>

        <StackPanel Orientation="Vertical" x:Name="xLayoutPanel" Width="200" Background="DodgerBlue">

        </StackPanel>
    </StackPanel>
</UserControl>
