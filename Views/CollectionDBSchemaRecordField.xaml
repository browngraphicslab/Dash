<UserControl
    x:Class="Dash.Views.CollectionDBSchemaRecordField"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash.Views"
    d:DataContext="{d:DesignInstance local:CollectionDBSchemaRecordFieldViewModel}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Width="{Binding Width}"
    Height="25"
    >
    <Border BorderThickness="{Binding BorderThickness}" Child="{Binding DataReference, Converter={StaticResource ReferenceFieldModelControllerToFrameworkElementConverter}, Mode=OneWay}">
        <!--TextBlock x:Name="xText" FontSize="12" Foreground="Black" Text="{Binding Content}"/-->
    </Border>
</UserControl>
