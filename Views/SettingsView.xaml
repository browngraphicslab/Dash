<Page
    x:Class="Dash.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="50">
        <StackPanel>
            <TextBlock FontWeight="SemiBold" FontSize="18">SETTINGS</TextBlock>

            <!--Style--> 
            <TextBlock FontSize="22" Margin="0,20,0,0">Customize your display</TextBlock>
            <ToggleSwitch Margin="10" IsOn="{x:Bind NightModeOn, Mode=TwoWay}">Night Mode</ToggleSwitch>
            <Slider Margin="10" Header="Font Size" Maximum="50" Minimum="5" HorizontalAlignment="Left" Width="150" Value="{x:Bind NoteFontSize, Mode=TwoWay}"></Slider>

            <TextBlock FontSize="22" Margin="0,20,0,0">Customize controls</TextBlock>
            <TextBlock FontSize="16">Mouse scroll functionality</TextBlock>
            <StackPanel Margin="10,0,0,0">
                <RadioButton IsChecked="{x:Bind MouseScrollOn, Mode=TwoWay}">Scroll</RadioButton>
                <RadioButton>Zoom</RadioButton>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
