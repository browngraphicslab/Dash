<Page
    x:Class="Dash.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:Dash"
    mc:Ignorable="d">

    <Grid x:Name="MyGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" SizeChanged="MyGrid_SizeChanged" >
        <Viewbox x:Name="xFreeFormView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
            <views:DocumentView x:Name="MainDocView"></views:DocumentView>
        </Viewbox>
                
                <!--local:FreeformView x:Name="xFreeformView" Width="Auto" Height="Auto" CanvasWidth="20000" CanvasHeight="20000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></ -->
        <local:OverlayCanvas x:Name="xOverlayCanvas"></local:OverlayCanvas>
    </Grid>
</Page>
