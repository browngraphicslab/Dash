<UserControl
    x:Class="DocumentMenu.OverlayMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DocumentMenu"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="xMenuGrid">
        <!--<Grid.RowDefinitions>
            <RowDefinition x:Name="xCollectionRow"/>
            <RowDefinition x:Name="xDocumentRow" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <StackPanel x:Name="xCollectionButtonsStackPanel" HorizontalAlignment="Left" Margin="0,0,10,0"></StackPanel>
        </Grid>
        <Grid Grid.Row="1">-->
        <StackPanel x:Name="xCollectionButtonsStackPanel" HorizontalAlignment="Left" Margin="{Binding ElementName=xDocumentButtonsStackPanel, Path=Margin}" Width="{Binding ElementName=xDocumentButtonsStackPanel, Path=Width}"></StackPanel>
            <StackPanel x:Name="xDocumentButtonsStackPanel" HorizontalAlignment="Left" Margin="0,0,10,0"></StackPanel>
        <!--</Grid>-->
    </Grid>
</UserControl>
