<UserControl
    x:Class="Dash.GraphNodeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="50" d:DesignHeight="400" d:DataContext="{d:DesignInstance local:GraphNodeViewModel}">

    <Grid x:Name="xGrid" Width="Auto" Height="Auto" Background="Transparent" IsHitTestVisible="False">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <!--tappable toggle ellipse-->
        <Ellipse Name="xEllipse" Fill="#CC114433" Grid.Row="0" Tapped="Node_OnTapped"/>
        <TextBlock  Name="xTitleBlock" Grid.Row="1" FontSize="20" HorizontalTextAlignment="Center" VerticalAlignment="Center"  HorizontalAlignment="Center"></TextBlock>
    </Grid>
</UserControl>
