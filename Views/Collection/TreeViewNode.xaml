<UserControl
    x:Class="Dash.TreeViewNode"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    DataContextChanged="TreeViewNode_OnDataContextChanged">

    <StackPanel>
        <Grid x:Name="XHeader">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="30"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="XTextBlock" Foreground="White" Grid.Column="0" DoubleTapped="XTextBlock_OnDoubleTapped"></TextBlock>
            <TextBlock x:Name="XArrowBlock" Foreground="White" Grid.Column="1" FontFamily="{StaticResource FontAwesome}" Style="{StaticResource xMenuItemExpandRight}" Tapped="XArrowBlock_OnTapped"></TextBlock>
        </Grid>
        <local:TreeViewCollectionNode x:Name="CollectionTreeView" Visibility="Collapsed" DataContext="{x:Null}" FilterString="{x:Bind FilterString}" ContainingDocument="{x:Bind ContainingDocument}"></local:TreeViewCollectionNode>
    </StackPanel>
</UserControl>
