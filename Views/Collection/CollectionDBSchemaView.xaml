<UserControl
    x:Class="Dash.CollectionDBSchemaView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:telerikGrid="using:Telerik.UI.Xaml.Controls.Grid"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    d:DataContext="{d:DesignInstance local:CollectionViewModel}"
    AllowDrop="True"
    mc:Ignorable="d"
                
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="xOuterGrid" Background="#CCFFFFFF" ManipulationMode="All" >
        <Grid.RowDefinitions>
            <RowDefinition x:Name="xDataArea" Height="*"></RowDefinition>
            <RowDefinition x:Name="xAddRowArea" Height="42"></RowDefinition>
        </Grid.RowDefinitions>
        <!--<telerikGrid:RadDataGrid x:Name="xDataGrid" Grid.Row="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"></telerikGrid:RadDataGrid>-->
        <controls:DataGrid x:Name="xDataGrid" Grid.Row="0" AutoGenerateColumns="False"></controls:DataGrid>

        <StackPanel HorizontalAlignment="Center" Grid.Row="1" Orientation="Horizontal">
            <Button HorizontalAlignment="Center" Tapped="AddRow_OnTapped" Margin="2" Width="200" Height="40">Add Row</Button>
            <Button HorizontalAlignment="Center" Tapped="AddColumn_OnTapped" Margin="2" Width="200" Height="40">Add Column</Button>
            <Button HorizontalAlignment="Center" Tapped="Flyout_OnTapped" Margin="2" Width="200" Height="40">
                <Button.Flyout>
                    <Flyout x:Name="xColumnFlyout" Placement="Bottom" Opening="XColumnFlyout_OnOpening">
                        <ItemsControl x:Name="xColumnsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="20"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <CheckBox Grid.Column="0" Loaded="CheckBox_OnLoaded"></CheckBox>
                                        <TextBlock Padding="10 5 5 5" Grid.Column="1" VerticalAlignment="Center" IsDoubleTapEnabled="False"  Text="{Binding}"></TextBlock>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Flyout>
                </Button.Flyout>
                Change Columns
            </Button>
        </StackPanel>
        
    </Grid>
 
</UserControl>
