<UserControl
    x:Class="Dash.CollectionDBSchemaHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    ManipulationMode="All"
    ManipulationStarted="UserControl_ManipulationStarted"
    AllowDrop="True"
    d:DesignHeight="300"
    d:DesignWidth="400" d:DataContext="{d:DesignInstance local:CollectionDBSchemaHeader+HeaderViewModel}"
    SizeChanged="DBS_OnSizeChanged">
    <Grid Width="{Binding Width}">
        <StackPanel Orientation="Horizontal">
            <Grid x:Name="NameGrid"  Tapped="SelectTap" DoubleTapped="TextBlock_DoubleTapped" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{ThemeResource DocumentBackground}">
                <TextBox x:Name="Text" IsHitTestVisible="False"  FontSize="12" FontWeight="Bold" Foreground="Black" Text="{Binding}"/>
            </Grid>
            <Grid Background="#CCDDDDDD" ManipulationMode="All" CanDrag="False" Width="7"
                  ManipulationStarted="Grid_ManipulationStarted" ManipulationDelta="Grid_ManipulationDelta" ManipulationCompleted="Grid_ManipulationCompleted"/>
        </StackPanel>
        </Grid> 
</UserControl>
