<UserControl
    x:Class="Dash.TimelineElement"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="50" d:DesignHeight="400" d:DataContext="{d:DesignInstance local:TimelineElementViewModel}">

    <Grid Background="Transparent" Tapped="TimelineElement_OnTapped">
        <Grid x:Name="xUpperDisplay" Margin="0,-95,0,0" Visibility="Visible" Height="315">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="45"></RowDefinition>
                <RowDefinition Height="20"></RowDefinition>
                <RowDefinition Height="100"></RowDefinition>
                <RowDefinition Height="100"></RowDefinition>

            </Grid.RowDefinitions>
            <Canvas Grid.Row="0" x:Name="xWebHolderTop" Margin="0,-35,0,0">
                <local:DocumentView x:Name="xDocumentPreview" Margin="30,0,0,0" Width="80" Height="80"
                    DataContext="{Binding DocumentViewModel}" 
                    Visibility="{x:Bind LocalContextVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" SizeChanged="DocumentPreviewSizeChanged">

                </local:DocumentView>
            </Canvas>
            <Rectangle Grid.Row="1" Width="2" Fill="LightGray" RadiusY="2" RadiusX="2" Height="80" Margin="0, 5, 1, 5" Visibility="Visible"></Rectangle>

            <Ellipse Grid.Row="2" Width="{x:Bind EllipseSize}" Height="{x:Bind EllipseSize}" Fill="DarkGreen"/>
            <Rectangle Grid.Row="3" Name="xLowerLine" Width="2" Fill="LightGray" Visibility="Collapsed" RadiusY="2" RadiusX="2" Height="80" Margin="0, 10, 1, 5"></Rectangle>
            <Canvas Grid.Row="4" x:Name="xWebHolder" Margin="15,-70,0,0"/>
        </Grid>
    </Grid>
</UserControl>
