<UserControl
    x:Class="Dash.Views.Collection.CollectionTreeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash.Views.Collection"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Rectangle Fill="White"></Rectangle>
        <TextBox x:Name="XFilterBox" Grid.Row="0" PlaceholderText="Filter text"></TextBox>
        <local:TreeViewCollectionNode Grid.Row="1"></local:TreeViewCollectionNode>
        <Button Grid.Row="2" HorizontalAlignment="Stretch" Height="40" Background="#747474">+ Add workspace</Button>
    </Grid>
</UserControl>
