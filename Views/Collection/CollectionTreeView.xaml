<UserControl
    x:Class="Dash.CollectionTreeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Background="White"
    d:DataContext="{d:DesignInstance local:CollectionViewModel}">
    <UserControl.Resources>
        <SolidColorBrush x:Key="ButtonBackgroundPointerOver">DarkGray</SolidColorBrush>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock TextAlignment="Center" FontSize="{StaticResource DefaultTitleFontSize}" Padding="0,7" Text="TreeView"/>
            <TextBox TextAlignment="Center" FontSize="{StaticResource DefaultTitleFontSize}" Text="TreeView" Padding="0,7"/>
        </Grid>
        <TextBox x:Name="XFilterBox" FontSize="12" Grid.Row="1" Margin="10" BorderThickness="1.5" PlaceholderText="filter notes"/>
        <local:TreeViewCollectionNode Margin="15 0 0 0" x:Name="xTreeRoot" HorizontalAlignment="Left" Grid.Row="2" FilterString="{Binding ElementName=XFilterBox, Path=Text, UpdateSourceTrigger=PropertyChanged}"/>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Grid.Row="3">
            <Button x:Name="makePdf" TabIndex="-1" HorizontalAlignment="Stretch" Padding="0,5" Tapped="MakePdf_OnTapped">Export to HTML
            </Button>
            <Button TabIndex="-1" HorizontalAlignment="Stretch" Padding="0,5" Tapped="UIElement_OnTapped">Add Workspace
            </Button>
        </StackPanel>

    </Grid>
</UserControl>
