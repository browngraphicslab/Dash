<UserControl
    x:Class="Dash.CollectionTreeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Background="White"
    d:DataContext="{d:DesignInstance local:CollectionViewModel}">
    <UserControl.Resources>
        <SolidColorBrush x:Key="ButtonBackgroundPointerOver">#686868</SolidColorBrush>
    </UserControl.Resources>

    <Grid Background="{ThemeResource TreeviewBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Rectangle Fill="White"></Rectangle>
        <Grid Background="{StaticResource WindowsBlue}" Grid.Row="0">
            <TextBlock TextAlignment="Center" FontSize="{StaticResource DefaultTitleFontSize}"  Foreground="#EEFFFF" Padding="0,7">My Workspaces</TextBlock>
    </Grid>
        <TextBox x:Name="XFilterBox" FontSize="12" Grid.Row="1" Margin="10" BorderBrush="#445555" Foreground="#001111" Background="#eeccdddd" BorderThickness="1.5" PlaceholderText="filter notes"></TextBox>
        <local:TreeViewCollectionNode Margin="15 0 0 0" x:Name="xTreeRoot" HorizontalAlignment="Left" Grid.Row="2" FilterString="{Binding ElementName=XFilterBox, Path=Text, UpdateSourceTrigger=PropertyChanged}"></local:TreeViewCollectionNode>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Grid.Row="3">
            <Button x:Name="makePdf" TabIndex="-1" HorizontalAlignment="Stretch" Background="{ThemeResource WorkspaceButtonBrush}" Foreground="#DDEEFFFF" Padding="0,5" Tapped="MakePdf_OnTapped">Export to HTML
            </Button>
            <Button TabIndex="-1" HorizontalAlignment="Stretch" Background="{ThemeResource WorkspaceButtonBrush}" Foreground="#DDEEFFFF" Padding="0,5" Tapped="UIElement_OnTapped">Add Workspace
            </Button>
        </StackPanel>

    </Grid>
</UserControl>
