<UserControl
    x:Class="Dash.CustomPdfView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Control"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <ScrollViewer x:Name="ScrollViewer" ViewChanging="ScrollViewer_OnViewChanging" LayoutUpdated="ScrollViewer_OnLayoutUpdated">
        <Grid>
            <ItemsControl x:Name="PageItemsControl" ItemsSource="{x:Bind Pages, Mode=OneWay}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel x:Name="xPageStackPanel" Orientation="Vertical" Spacing="{Binding PageSpacing, ElementName=Control}" ></StackPanel>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
            <local:AnnotationOverlay x:Name="xAnnotations" PointerPressed="ItemsControl_PointerPressed" PointerReleased="ItemsControl_PointerReleased"></local:AnnotationOverlay>
            <Viewbox HorizontalAlignment="Stretch" Stretch="UniformToFill">
                <Canvas x:Name="TestSelectionCanvas"></Canvas>
            </Viewbox>
        </Grid>
    </ScrollViewer>
</UserControl>
